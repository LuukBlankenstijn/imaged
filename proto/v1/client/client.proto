syntax = "proto3";

package client.v1;

message StartStreamRequest {
  string mac = 1;
  uint64 disk_size_bytes = 2;
}

message Task {}

service ClientService {
  rpc StartStream(StartStreamRequest) returns (stream Task) {}
}
